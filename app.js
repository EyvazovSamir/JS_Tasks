let width = prompt("Enter width:");
let height = prompt("Enter height:");
let area = Number(width) * Number(height);
alert(area);

let birthDate = prompt("Enter your birth date:");
let currentYear = prompt("Enter current year:");
let age = Number(currentYear) - Number(birthDate);
alert(`${age} years`);

let km = prompt("Enter km:");
const mil = 0.6214;
let userDeyeri = Number(mil) * Number(km);
alert(`${userDeyeri} mil`);
